# Legacy scrapers (archived workflow)

The active ingestion workflow is now `backend/ingestion/` (`py -m ingestion.run`).

This `scrapers/` tree is kept for historical reference and emergency fallback.
Do not add new ingestion logic here.
